# @package _global_
model_args:
  model_name_or_path: "Qwen/Qwen3-0.6B"

data_args:
  dataset_name: "thavens/Qwen3-0.6B-secalign-tools"

trainer_args:
  run_name: q3_0.6b_secalign_tools_ld_0.1_ad_0.1
  output_dir: outputs/${trainer_args.run_name}

  per_device_train_batch_size: 8
  gradient_accumulation_steps: 4

