# @package _global_
model_args:
  model_name_or_path: "Qwen/Qwen3-4B"

data_args:
  dataset_name: "thavens/Qwen3-4B-secalign-tools-thinking"

format_args:
  reasoning_only: true

trainer_args:
  run_name: q3_4b_secalign_tools_ro
  output_dir: outputs/${trainer_args.run_name}

  per_device_train_batch_size: 4
  gradient_accumulation_steps: 8

  max_length: 4096

